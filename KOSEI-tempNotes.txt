Calendar notes;TODO :

-Authentication Guard (_login.php),
blocks unauthenticated users from loading the visual calendar interface.
(Security constraint: Must be included at the very top of schedule.php).

-Backend Session Verification,
prevents unauthorized external POST requests from altering the JSON file.
CalData_events.php must check $_SESSION['UserLogin'] before executing Create/Delete logic

-Directory Protection (.htaccess / Server Config),
prevents navigating directly to the secure folder via the browser.(?)
I didnt look in the other files much, not sure if this is done this already

-Event Description(?) we can add this feature, lets talk about what to add
next class